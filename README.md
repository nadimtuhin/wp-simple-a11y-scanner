# WP Simple A11y Scanner

A clean, testable WordPress plugin that scans post content for accessibility problems (e.g., missing alt attributes on images).

## Running Tests

```bash
composer install
./vendor/bin/phpunit
```
